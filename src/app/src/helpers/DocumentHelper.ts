import { ITreeNode } from "../view-models/tree-view/interfaces/ITreeNode";
import { IDocument } from "../contracts/interfaces/IDocument";

export function getTree(documents: IDocument[]): ITreeNode[][] {

    for (const document in documents) {

    }


    return [][];
}
